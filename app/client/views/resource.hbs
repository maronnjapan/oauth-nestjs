<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>OAuth in Action: OAuth Client</title>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <style>
        body {
            padding-top: 60px;
        }

        .navbar-inverse {
            background-color: #223;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">OAuth in Action: <span class="label label-primary">OAuth
                        Client</span></a>
            </div>
        </div>
    </nav>

    <div class="container">

        <div class="row">

            <div class="col-md-4">
                <div class="well">
                    <h3>Read the current value</h3>
                    {{#if readSuccess}}<p><span class="label label-success">Success</span></p>{{/if}}
                    {{#if readFail}}<p><span class="label label-danger">Failure</span></p>{{/if}}
                    <p>{{#if words}} Words: {{words}} {{else}} {{/if}}</p>
                    <a class="btn btn-info" href="/fetch-resource/read">GET current value</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="well">
                    <h3>Add a word to the list</h3>
                    {{#if addSuccess}}<p><span class="label label-success">Success</span></p>{{/if}}
                    {{#if addFail}}<p><span class="label label-danger">Failure</span></p>{{/if}}
                    <form class="form" action="/fetch-resource/add" method="GET">
                        <input type="text" name="word" placeholder="word">
                        <input type="submit" class="btn btn-warning" href="/" value="POST a new word">
                    </form>
                </div>
            </div>
            <div class="col-md-4">
                <div class="well">
                    <h3>Remove the last word from the list</h3>
                    {{#if deleteSuccess}}<p><span class="label label-success">Success</span></p>{{/if}}
                    {{#if deleteFail}}<p><span class="label label-danger">Failure</span></p>{{/if}}
                    <a class="btn btn-danger" href="/fetch-resource/delete">DELETE the last word</a>
                </div>
            </div>

        </div>

    </div><!-- /.container -->



    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
</body>

</html>